rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /expenseDashboards/{dashboardId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;

      match /members/{memberId} {
        allow read: if true;
        allow create, update, delete: if request.auth != null;
      }

      match /expenses/{expenseId} {
        allow read: if true;
        allow create, update, delete: if request.auth != null;
      }
    }
  }
}
